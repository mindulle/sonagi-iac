version: '3.8'

services:
  ops-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sonagi-ops-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV={{ node_env }}
      - LOG_LEVEL={{ log_level }}
      - APP_NAME=ops-bot
    mem_limit: 512m
    mem_reservation: 256m
    cpus: 0.5
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
    networks:
      - sonagi-network

  media-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sonagi-media-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV={{ node_env }}
      - LOG_LEVEL={{ log_level }}
      - APP_NAME=media-bot
      - LAVALINK_URL={{ lavalink_url }}
      - LAVALINK_PASSWORD={{ lavalink_password }}
      - LAVALINK_SECURE={{ lavalink_secure }}
    mem_limit: 512m
    mem_reservation: 256m
    cpus: 0.5
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
    networks:
      - sonagi-network
    depends_on:
      - lavalink

  dev-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sonagi-dev-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV={{ node_env }}
      - LOG_LEVEL={{ log_level }}
      - APP_NAME=dev-bot
    mem_limit: 512m
    mem_reservation: 256m
    cpus: 0.5
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
    networks:
      - sonagi-network

  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:4
    container_name: lavalink
    restart: unless-stopped
    environment:
      - SERVER_PORT=2333
      - LAVALINK_SERVER_PASSWORD={{ lavalink_password }}
    networks:
      - sonagi-network
    ports:
      - '2333:2333'

networks:
  sonagi-network:
    driver: bridge
