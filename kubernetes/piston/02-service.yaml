apiVersion: v1
kind: Service
metadata:
  name: piston
  labels:
    app: piston
spec:
  type: ClusterIP
  ports:
    - port: 2000
      targetPort: 2000
      protocol: TCP
  selector:
    app: piston
