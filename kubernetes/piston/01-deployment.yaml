apiVersion: apps/v1
kind: Deployment
metadata:
  name: piston
  labels:
    app: piston
spec:
  replicas: 1
  selector:
    matchLabels:
      app: piston
  template:
    metadata:
      labels:
        app: piston
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: piston
          image: ghcr.io/mindulle/piston:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 2000
          securityContext:
            privileged: true
